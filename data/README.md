# Data

Hotel review corpus is stored in **Supabase** (places, reviews, embeddings).

Local files in this folder (seed CSV, discovery catalogs, Chrome profile) are used only by gitignored crawl scripts on a developer machine and are not pushed to GitHub.
